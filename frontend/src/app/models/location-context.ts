/** User-selected retail context — persisted in localStorage. */
export class LocationContext {
  companyid: number | null = null;
  companyName = '';
  locationid: number | null = null;
  locationName = '';
  counterid: number | null = null;
  counterName = '';
}
